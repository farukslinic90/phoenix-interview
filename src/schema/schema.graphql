scalar Message

type Query {
  user: User
}

type Mutation {
  updateUser(message: Message!): User
}

type User {
  id: Int
  uid: String
  first_name: String
  last_name: String
  username: String
  email: String
  message: String
}
